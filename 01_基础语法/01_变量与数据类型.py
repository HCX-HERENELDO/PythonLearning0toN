#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
============================================================================
模块名称：变量与数据类型
学习目标：理解 Python 变量的概念，掌握基本数据类型的使用
PyCharm 技巧：学习如何查看变量的值和类型
============================================================================
"""

# ============================================================================
# 第一部分：什么是变量？
# ============================================================================
"""
【概念讲解】
变量就像一个"盒子"，用来存储数据。
在 Python 中，变量不需要声明类型，直接赋值即可使用。

变量命名规则：
1. 只能包含字母、数字和下划线
2. 不能以数字开头
3. 不能使用 Python 关键字（如 if、for、while 等）
4. 区分大小写（name 和 Name 是不同的变量）
5. 建议使用有意义的名称，提高代码可读性
"""

# ----------------------------------------------------------------------------
# 1.1 变量的创建和赋值
# ----------------------------------------------------------------------------

# 创建变量的基本语法：变量名 = 值
name = "张三"           # 字符串变量
age = 25               # 整数变量
height = 1.75          # 浮点数变量
is_student = True      # 布尔变量

# 打印变量的值
print("姓名:", name)
print("年龄:", age)
print("身高:", height, "米")
print("是否学生:", is_student)

# 【PyCharm 技巧】
# 将鼠标悬停在变量上，可以看到变量的类型提示
# 按 Ctrl + 点击变量名，可以查看变量的定义位置

# ----------------------------------------------------------------------------
# 1.2 变量的重新赋值
# ----------------------------------------------------------------------------

# Python 变量可以随时改变存储的值，甚至改变类型
x = 10
print("x 的值:", x, "类型:", type(x))

x = "Hello"  # 变量类型从整数变为字符串
print("x 的值:", x, "类型:", type(x))

# 【注意】虽然可以这样，但不推荐频繁改变变量类型，会导致代码难以理解

# ============================================================================
# 第二部分：基本数据类型
# ============================================================================

# ----------------------------------------------------------------------------
# 2.1 整数
# ----------------------------------------------------------------------------

# 整数就是没有小数部分的数字
positive_int = 100      # 正整数
negative_int = -50      # 负整数
zero = 0               # 零

# Python 支持大整数，不会溢出
big_number = 123456789012345678901234567890
print("大整数:", big_number)

# 不同进制的整数表示
binary_num = 0b1010     # 二进制，以 0b 开头
octal_num = 0o12        # 八进制，以 0o 开头
hex_num = 0xFF          # 十六进制，以 0x 开头

print(f"二进制 {binary_num}, 八进制 {octal_num}, 十六进制 {hex_num}")

# 进制转换函数
print("十进制转二进制:", bin(10))      # 0b1010
print("十进制转八进制:", oct(10))      # 0o12
print("十进制转十六进制:", hex(255))    # 0xff

# ----------------------------------------------------------------------------
# 2.2 浮点数
# ----------------------------------------------------------------------------

# 浮点数就是带有小数部分的数字
pi = 3.14159
e = 2.71828
temperature = -17.5

# 科学计数法表示
large_float = 1.23e10    # 1.23 × 10^10 = 12300000000.0
small_float = 1.23e-5    # 1.23 × 10^-5 = 0.0000123

print(f"科学计数法: {large_float}, {small_float}")

# 浮点数精度问题（经典面试题）
print(0.1 + 0.2)  # 输出 0.30000000000000004，而不是 0.3
# 这是因为计算机使用二进制存储浮点数，存在精度问题

# 解决精度问题：使用 decimal 模块
from decimal import Decimal
print(Decimal('0.1') + Decimal('0.2'))  # 精确输出 0.3

# ----------------------------------------------------------------------------
# 2.3 字符串
# ----------------------------------------------------------------------------

# 字符串是用引号包围的文本
single_quote = '单引号字符串'
double_quote = "双引号字符串"
triple_quote = """
多行字符串，
可以跨越多行，
保留格式。
"""

print(single_quote)
print(double_quote)
print(triple_quote)

# 字符串的转义字符
escape_str = "Hello\nWorld\tPython"  # \n 换行，\t 制表符
print(escape_str)

# 原始字符串（不转义）
raw_str = r"C:\Users\name\folder"  # 前面加 r，反斜杠不会被转义
print(raw_str)

# 字符串索引和切片
greeting = "Hello, Python!"
print("第一个字符:", greeting[0])       # H
print("最后一个字符:", greeting[-1])    # !
print("前5个字符:", greeting[:5])       # Hello
print("从第7个开始:", greeting[7:])     # Python!
print("反转字符串:", greeting[::-1])    # !nohtyP ,olleH

# 字符串常用方法
text = "  Hello, World!  "
print("去除空格:", text.strip())
print("转大写:", text.upper())
print("转小写:", text.lower())
print("替换:", text.replace("World", "Python"))
print("分割:", text.split(","))
print("查找:", text.find("World"))

# 字符串格式化（三种方式）
name = "小明"
age = 20

# 方式1：f-string（推荐，Python 3.6+）
print(f"我叫{name}，今年{age}岁")

# 方式2：format 方法
print("我叫{}，今年{}岁".format(name, age))
print("我叫{n}，今年{a}岁".format(n=name, a=age))

# 方式3：百分号格式化（旧式）
print("我叫%s，今年%d岁" % (name, age))

# 格式化数字
price = 19.99
print(f"价格: ¥{price:.2f}")    # 保留两位小数
print(f"百分比: {0.856:.1%}")   # 百分比格式

# ----------------------------------------------------------------------------
# 2.4 布尔类型
# ----------------------------------------------------------------------------

# 布尔类型只有两个值：True 和 False
is_true = True
is_false = False

# 布尔值常用于条件判断
if is_true:
    print("这是真的！")

# 哪些值会被视为 False？
# 以下值在布尔上下文中都被视为 False（称为"假值"）
print("布尔转换测试:")
print("bool(0):", bool(0))           # False
print("bool(0.0):", bool(0.0))       # False
print("bool(''):", bool(''))         # False - 空字符串
print("bool([]):", bool([]))         # False - 空列表
print("bool(None):", bool(None))     # False

# 其他值都被视为 True
print("bool(1):", bool(1))           # True
print("bool(-1):", bool(-1))         # True
print("bool('a'):", bool('a'))       # True
print("bool([1]):", bool([1]))       # True

# ----------------------------------------------------------------------------
# 2.5 None 类型
# ----------------------------------------------------------------------------

# None 表示"无"或"空"，类似于其他语言的 null
result = None

# 常用于：
# 1. 表示函数没有返回值
# 2. 表示变量还没有值
# 3. 作为默认参数值

def find_item(lst, target):
    """查找列表中的元素，找到返回索引，找不到返回 None"""
    for i, item in enumerate(lst):
        if item == target:
            return i
    return None

index = find_item([1, 2, 3], 5)
if index is None:  # 推荐使用 is 而不是 == 来比较 None
    print("未找到元素")

# ============================================================================
# 第三部分：类型检查与类型转换
# ============================================================================

# ----------------------------------------------------------------------------
# 3.1 类型检查
# ----------------------------------------------------------------------------

# 使用 type() 函数查看变量的类型
num = 42
text = "Hello"
flag = True

print(type(num))    # <class 'int'>
print(type(text))   # <class 'str'>
print(type(flag))   # <class 'bool'>

# 使用 isinstance() 检查变量是否属于某种类型
print(isinstance(num, int))      # True
print(isinstance(num, str))      # False
print(isinstance(num, (int, float)))  # True，检查是否为 int 或 float

# 【PyCharm 技巧】
# 在调试模式下，可以在 Variables 面板查看所有变量的类型和值
# 按 Alt + F8 可以打开 Evaluate Expression 窗口，执行任意代码

# ----------------------------------------------------------------------------
# 3.2 类型转换
# ----------------------------------------------------------------------------

# 字符串转整数
str_num = "123"
int_num = int(str_num)
print(f"字符串 '{str_num}' 转整数: {int_num}, 类型: {type(int_num)}")

# 浮点数转整数（截断小数部分）
float_num = 3.7
int_from_float = int(float_num)
print(f"浮点数 {float_num} 转整数: {int_from_float}")  # 3，不是四舍五入

# 要四舍五入，使用 round()
print(f"四舍五入: {round(float_num)}")  # 4

# 整数转浮点数
int_val = 10
float_val = float(int_val)
print(f"整数 {int_val} 转浮点数: {float_val}")

# 数字转字符串
num = 42
str_from_num = str(num)
print(f"数字 {num} 转字符串: '{str_from_num}'")

# 布尔值转换
print(int(True))    # 1
print(int(False))   # 0
print(bool(1))      # True
print(bool(0))      # False

# ============================================================================
# 第四部分：输入与输出
# ============================================================================

# ----------------------------------------------------------------------------
# 4.1 输出 - print() 函数
# ----------------------------------------------------------------------------

# 基本 print
print("Hello, World!")

# 打印多个值
print("姓名:", "张三", "年龄:", 25)

# 自定义分隔符
print("2023", "12", "25", sep="-")  # 2023-12-25

# 自定义结束符
print("Hello", end=" ")
print("World")  # 输出: Hello World（同一行）

# 打印到文件
# with open("output.txt", "w", encoding="utf-8") as f:
#     print("写入文件的内容", file=f)

# ----------------------------------------------------------------------------
# 4.2 输入 - input() 函数
# ----------------------------------------------------------------------------

# input() 总是返回字符串类型
# name = input("请输入您的姓名: ")
# print(f"您好, {name}!")

# 获取数字输入需要类型转换
# age_str = input("请输入您的年龄: ")
# age = int(age_str)
# print(f"您明年将 {age + 1} 岁")

# 更安全的方式（处理可能的输入错误）
# try:
#     age = int(input("请输入年龄: "))
#     print(f"您的年龄是: {age}")
# except ValueError:
#     print("请输入有效的数字！")

# ============================================================================
# 第五部分：常量与命名规范
# ============================================================================

# Python 没有真正的常量，约定用全大写表示常量
PI = 3.14159
MAX_SIZE = 100
DEFAULT_NAME = "Guest"

# 虽然可以修改，但不应该修改"常量"的值
# PI = 3.14  # 不推荐！

# 命名规范总结
"""
命名风格：
1. 变量和函数：小写字母，下划线分隔（snake_case）
   例如：user_name, total_count, calculate_sum

2. 常量：全大写，下划线分隔
   例如：MAX_VALUE, PI, DEFAULT_TIMEOUT

3. 类名：大驼峰命名
   例如：MyClass, StudentInfo, HttpServer

4. 私有变量：以单下划线开头
   例如：_private_var

5. 强私有变量：以双下划线开头
   例如：__very_private

6. 特殊方法：双下划线包围
   例如：__init__, __str__
"""

# ============================================================================
# 练习题
# ============================================================================
"""
【练习1】创建以下变量：
- 存储你的姓名（字符串）
- 存储你的年龄（整数）
- 存储你的身高（浮点数）
- 存储你是否是学生（布尔值）
然后打印这些变量及其类型。

【练习2】字符串操作：
- 创建一个包含你名字的字符串
- 将名字转换为大写和小写
- 计算名字的长度
- 将名字反转

【练习3】类型转换：
- 将字符串 "3.14" 转换为浮点数
- 将浮点数 3.14 转换为整数
- 将整数 100 转换为字符串

【练习4】输入输出：
- 编写程序，让用户输入姓名和年龄
- 计算并输出用户出生年份（假设今年是2024年）
"""

# ============================================================================
# 本节小结
# ============================================================================
"""
✅ 掌握的知识点：
1. 变量的概念和命名规则
2. 四种基本数据类型：int, float, str, bool
3. None 类型的使用
4. type() 和 isinstance() 类型检查
5. 各种类型之间的转换
6. input() 和 print() 的使用
7. 字符串格式化
8. 命名规范

🔧 PyCharm 技巧：
1. 鼠标悬停查看变量类型
2. Ctrl + 点击跳转到定义
3. Alt + F8 执行表达式
4. Variables 面板查看变量

➡️ 下一节：运算符
"""

if __name__ == "__main__":
    print("\n" + "=" * 60)
    print("变量与数据类型模块学习完成！")
    print("=" * 60)